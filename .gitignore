# Node/NPM Artifacts
node_modules
.pnpm-debug.log
.yarn/cache
*.tsbuildinfo

# React Native / Expo Specific
/ios/build
/android/build
/android/app/build
/android/build
.expo-shared
/dist
/build

# Build Output
/coverage
*.log

# Editor/IDE Files
.vscode/
.idea/
*.swp
.DS_Store

# Dependency Lock Files (Managed by uv/npm, keeping only essential for initial clone)
# npm list --depth=0 > package-lock.json (If using npm, but keeping clean for a standard JS/RN repo)

# Environment Variables (Sensitive data)
.env
.env.local

# Supabase CLI local output
.supabase/types

# Gemini Cache/Logs (If any large caches are generated)
*.gemini_cache
